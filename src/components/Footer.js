import React from "react"
import styled from "styled-components"

const Footer = () => {
  return <FooterStyle className="fw">{/*  */}</FooterStyle>
}

export default Footer

const FooterStyle = styled.footer`
  background: var(--lightBlue);
  height: 18.2rem;
  margin: 0 auto 4.4rem;
`
